{
  "name": "single",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm test && http-server --i=false -o -p 3000",
    "test": "gulp lint && webpack"
  },
  "dependencies": {
  },
  "devDependencies": {
    "chai": "^3.4.0",
    "gulp": "^3.9.0",
    "gulp-eslint": "^1.0.0",
    "mocha": "^2.3.3",
    "webpack": "^1.12.2"
  }
}
